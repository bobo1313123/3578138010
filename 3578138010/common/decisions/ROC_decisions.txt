ROC_Fan_Asian ={
	BOTH_decision_one ={
		cost = 45
		fire_only_once = no

		complete_effect = {
			add_war_support = 0.02
			randomize_temp_variable = {
				var = randomFAS
				distribution = uniform
				min = 0
				max = 1.4
			}
			add_to_variable = {
				Fan_Asian_support = randomFAS
			}
			clamp_variable = {
				var = Fan_Asian_support
				min = 0
				max = 100
			}
			clear_variable = Fan_Asian_support_st
			add_to_variable = {
				var = Fan_Asian_support_st
				value = Fan_Asian_support
			}
			divide_variable = { Fan_Asian_support_st = 1000 }
			clear_variable = Fan_Asian_support_pp
			add_to_variable = {
				var = Fan_Asian_support_pp
				value = Fan_Asian_support
			}
			divide_variable = { Fan_Asian_support_pp = 1500 }
			custom_effect_tooltip = ROC_Fan_Asian_support_small_tt
		}
	}
}
ROC_National_Assembly = {
	CHI_decision_one ={
		cost = 45
		fire_only_once = no
		complete_effect = {
			add_war_support = 0.02
			add_stability = 0.03
		}
	}
}